# Notebooks

Volontairement vide : l'exploration interactive se fait dans le dashboard Streamlit
(`streamlit run app/dashboard.py`), et la logique vit dans `src/rte_forecast/`.
Ajoutez ici des notebooks d'analyse ad hoc en important `rte_forecast` (installé en mode editable).
